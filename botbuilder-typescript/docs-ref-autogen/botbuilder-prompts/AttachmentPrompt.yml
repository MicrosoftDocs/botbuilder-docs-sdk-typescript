### YamlMime:UniversalReference
items:
  - uid: botbuilder-prompts.AttachmentPrompt
    name: AttachmentPrompt
    fullName: AttachmentPrompt<O>
    children:
      - botbuilder-prompts.AttachmentPrompt.prompt
      - botbuilder-prompts.AttachmentPrompt.recognize
    langs:
      - typeScript
    type: interface
    summary: Prompts the user to upload one or more attachments.
    package: botbuilder-prompts
    remarks: |-
      This example shows how to create a new attachment prompt:

      ```JavaScript
      const { createAttachmentPrompt } = require('botbuilder-prompts');

      const imagePrompt = createAttachmentPrompt();
      ```
  - uid: botbuilder-prompts.AttachmentPrompt.prompt
    name: prompt
    children: []
    type: method
    langs:
      - typeScript
    summary: Sends a formated prompt to the user.
    syntax:
      content: >-
        function prompt(context: TurnContext, prompt: string |
        Partial<Activity>, speak?: string)
      parameters:
        - id: context
          type:
            - TurnContext
          description: Context for the current turn of conversation.
        - id: prompt
          type:
            - string | Partial<Activity>
          description: Text or activity to send as the prompt.
        - id: speak
          type:
            - string
          description: >
            (Optional) SSML that should be spoken for prompt. The prompts
            `inputHint` will be automatically set to `expectingInput`.
          optional: true
      return:
        type:
          - Promise<void>
    package: botbuilder-prompts
    remarks: |-
      This example shows how to send a prompt to the user:

      ```JavaScript
      await imagePrompt.prompt(context, `Upload an image(s).`);
      ```
  - uid: botbuilder-prompts.AttachmentPrompt.recognize
    name: recognize
    children: []
    type: method
    langs:
      - typeScript
    summary: Recognizes and validates the users reply.
    syntax:
      content: 'function recognize(context: TurnContext)'
      parameters:
        - id: context
          type:
            - TurnContext
          description: |
            Context for the current turn of conversation.
      return:
        type:
          - Promise<O | undefined>
    package: botbuilder-prompts
    remarks: >-
      The result of the call will either be the recognized value or `undefined`.


      The recognize() method will not automatically re-prompt the user so either
      the caller or the

      prompts custom validator will need to implement re-prompting logic.


      ```JavaScript

      const images = await imagePrompt.recognize(context);

      if (images) {
         // Process images
      }

      ```
